# داده‌های پروژه

این پوشه شامل تمام فایل‌های داده پروژه است.

## ساختار

```
data/
├── raw/                              # داده‌های خام
│   ├── taxi_zone_lookup_coordinates.csv
│   └── weather_data_cleaned.csv
└── README.md
```

## داده‌های خام (raw/)

### 📍 `taxi_zone_lookup_coordinates.csv`

اطلاعات مناطق تاکسی و مختصات جغرافیایی

**ستون‌ها:**

- `location_id`: شناسه منحصر به فرد منطقه
- `zone_name`: نام منطقه
- `borough`: بخش شهری (Manhattan, Brooklyn, Queens, ...)
- `latitude`: عرض جغرافیایی
- `longitude`: طول جغرافیایی

**تعداد رکوردها**: ~260 منطقه

---

### 🌦️ `weather_data_cleaned.csv`

داده‌های آب و هوای تمیز شده

**ستون‌ها:**

- `date`: تاریخ (YYYY-MM-DD)
- `temperature`: دما (درجه سانتی‌گراد)
- `humidity`: رطوبت (درصد)
- `wind_speed`: سرعت باد (km/h)
- `precipitation`: بارندگی (mm)
- `weather_condition`: وضعیت آب و هوا (Clear, Rain, Snow, ...)

**بازه زمانی**: [تکمیل شود]
**تعداد رکوردها**: [تکمیل شود]

---

## پردازش داده‌ها

برای پردازش داده‌های خام:

### روش ۱: استفاده از Pipeline

```bash
python pipeline.py
```

### روش ۲: استفاده از Notebooks

```bash
jupyter notebook notebooks/
```

## خروجی‌ها

فایل‌های پردازش شده در پوشه `../output/` ذخیره می‌شوند:

- `processed_uber_trips.csv`
- `processed_weather_data.csv`
- `processed_taxi_zones.csv`

## منابع داده

### داده‌های Uber

- منبع: NYC Taxi and Limousine Commission (TLC)
- وب‌سایت: https://www1.nyc.gov/site/tlc/

### داده‌های آب و هوا

- منبع: NOAA (National Oceanic and Atmospheric Administration)
- وب‌سایت: https://www.ncdc.noaa.gov/

### مختصات جغرافیایی

- منبع: NYC Open Data
- وب‌سایت: https://opendata.cityofnewyork.us/

## نکات مهم

1. **حجم داده‌ها**: برخی فایل‌ها ممکن است حجم بالایی داشته باشند
2. **فرمت تاریخ**: تمام تاریخ‌ها در فرمت ISO (YYYY-MM-DD) هستند
3. **کدگذاری**: تمام فایل‌ها UTF-8 هستند
4. **جداکننده**: کاراکتر `,` (کاما) به عنوان جداکننده CSV
5. **مقادیر گمشده**: با `NaN` یا سلول خالی نشان داده می‌شوند

## دستورات مفید

### مشاهده سریع داده‌ها

```bash
# چند خط اول
head -n 10 raw/taxi_zone_lookup_coordinates.csv

# تعداد خطوط
wc -l raw/*.csv

# جستجو در داده‌ها
grep "Manhattan" raw/taxi_zone_lookup_coordinates.csv
```

### با Python

```python
import pandas as pd

# خواندن داده
df = pd.read_csv('raw/weather_data_cleaned.csv')

# اطلاعات کلی
print(df.info())
print(df.describe())
print(df.head())
```

## به‌روزرسانی داده‌ها

برای به‌روزرسانی داده‌ها:

1. دانلود داده‌های جدید از منابع
2. قرار دادن در پوشه `raw/`
3. اجرای notebook های پیش‌پردازش
4. اجرای pipeline

## پشتیبان‌گیری

توصیه می‌شود قبل از هر پردازش، از داده‌های خام backup بگیرید:

```bash
tar -czf data_backup_$(date +%Y%m%d).tar.gz raw/
```
