# Jupyter Notebooks

این پوشه شامل Jupyter Notebook های پروژه است.

## فایل‌ها

### 📓 `import.ipynb`
- **هدف**: وارد کردن داده‌ها به دیتابیس MySQL
- **ورودی**: فایل‌های CSV خام از پوشه `data/raw/`
- **خروجی**: جداول MySQL (uber_trips, weather_data, taxi_zones)

**نحوه استفاده:**
```python
# اتصال به دیتابیس
# خواندن فایل‌های CSV
# وارد کردن داده‌ها به MySQL
```

---

### 🧹 `clean.ipynb`
- **هدف**: تمیز کردن و پیش‌پردازش داده‌های Uber
- **ورودی**: داده‌های خام Uber
- **خروجی**: داده‌های تمیز شده

**عملیات:**
- حذف مقادیر گمشده
- تبدیل فرمت تاریخ
- استانداردسازی نام ستون‌ها
- حذف رکوردهای تکراری
- اعتبارسنجی داده‌ها

---

### 🌐 `scrapping.ipynb`
- **هدف**: جمع‌آوری داده از وب
- **منابع**: وب‌سایت‌های مختلف برای داده‌های آب و هوا و جغرافیایی
- **خروجی**: فایل‌های CSV

**تکنولوژی:**
- BeautifulSoup / Selenium
- requests
- pandas

---

### 🌦️ `Weather_Complete_Report_Final.ipynb`
- **هدف**: تحلیل کامل داده‌های آب و هوا
- **شامل**:
  - تحلیل آماری توصیفی
  - بررسی توزیع متغیرهای آب و هوایی
  - تحلیل همبستگی
  - تحلیل روندهای زمانی
  - تصویرسازی داده‌ها

**خروجی:**
- نمودارها و گراف‌ها
- گزارش آماری
- داده‌های تمیز شده

---

## راه‌اندازی

### نصب Jupyter

```bash
pip install jupyter notebook
```

### اجرا

```bash
jupyter notebook
```

یا با JupyterLab:

```bash
pip install jupyterlab
jupyter lab
```

## وابستگی‌ها

همه وابستگی‌های مورد نیاز در فایل `requirements.txt` موجود است:

```bash
pip install -r ../requirements.txt
```

## توصیه‌ها

1. **ترتیب اجرا**:
   - ابتدا `scrapping.ipynb` (در صورت نیاز)
   - سپس `clean.ipynb`
   - سپس `import.ipynb`
   - در نهایت `Weather_Complete_Report_Final.ipynb`

2. **محیط مجازی**: استفاده از virtual environment توصیه می‌شود

3. **نسخه Python**: Python 3.8 یا بالاتر

4. **حافظه**: برخی notebook ها ممکن است نیاز به حافظه بالا داشته باشند

## نکات

- همه notebook ها به صورت تعاملی قابل اجرا هستند
- می‌توانید کدها را بخش به بخش اجرا کنید
- برای دیباگ، از print و display استفاده کنید
- خروجی‌ها در پوشه `output/` ذخیره می‌شوند

